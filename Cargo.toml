[package]
name = "bunsenite"
version = "1.0.2"
authors = ["Campaign for Cooler Coding and Programming"]
edition = "2021"
rust-version = "1.90"
description = "Nickel configuration file parser with multi-language FFI bindings"
documentation = "https://docs.rs/bunsenite"
repository = "https://github.com/hyperpolymath/bunsenite"
license = "MPL-2.0"
keywords = ["nickel", "config", "parser", "ffi", "wasm"]
categories = ["config", "parsing", "wasm", "api-bindings"]
readme = "README.adoc"
exclude = [
    "/.git",
    "/.gitlab",
    "/target",
    "/examples/*/target",
]

[lib]
name = "bunsenite"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "bunsenite"
path = "src/main.rs"

[dependencies]
# Parsing/evaluation does not use Nickel's Markdown, documentation, REPL or
# formatter features. Avoid their optional dependency trees (and advisories).
nickel-lang-core = { version = "0.19.0", default-features = false }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
anyhow = "1.0"
thiserror = "1.0"
miette = { version = "7.0", features = ["fancy"] }

# Watch mode
notify = { version = "6.1", optional = true }

# REPL
rustyline = { version = "15.0", optional = true }

# Schema validation
jsonschema = { version = "0.18", optional = true }

# CLI (optional, for binary only)
clap = { version = "4.6", features = ["derive", "cargo"], optional = true }

# WASM support
[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
# Note: wee_alloc was removed as it is unmaintained and has known memory leaks.
# Rust 1.71+ provides a suitable default allocator for wasm32 targets.

# Until upstream nickel-lang-vector supports imbl-sized-chunks 0.2, use its
# unchanged 0.2.0 sources with only that dependency updated. See the local
# crate README for provenance and the plan to remove this override.
[patch.crates-io]
nickel-lang-vector = { path = "crates/nickel-lang-vector" }

[dev-dependencies]
# Testing
pretty_assertions = "1.4"
tempfile = "3.27"
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "parser"
harness = false

[[bench]]
name = "bunsenite_bench"
harness = false

[features]
default = ["cli"]
cli = ["dep:clap"]
wasm = []
watch = ["dep:notify", "cli"]
repl = ["dep:rustyline", "cli"]
schema = ["dep:jsonschema"]
full = ["cli", "watch", "repl", "schema"]

# Offline-first: No network dependencies, all features work air-gapped
# Type safety: Rust compiler guarantees
# Memory safety: Rust ownership model, zero unsafe blocks (enforced in CI)

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"

[profile.release-with-debug]
inherits = "release"
strip = false
debug = true

# WASM optimization
[profile.wasm-release]
inherits = "release"
opt-level = "z"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

# Security audit configuration
[package.metadata.audit]
# Ignore advisories that don't apply
ignore = []

# RSR Framework Metadata
[package.metadata.rsr]
tier = "bronze"
compliance-version = "1.0.0"
offline-first = true
type-safety = "compile-time"
memory-safety = "rust-ownership"
tpcf-perimeter = 3  # Community Sandbox
security-contact = "https://github.com/hyperpolymath/bunsenite/security/advisories/new"
